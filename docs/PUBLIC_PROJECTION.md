# Public projection record

Projection date: 24 August 2026.

This lightweight GitHub tree was selected from the verified reviewer reproducibility archive. It retains the executable protocol, simulations, independent validation, compact provider audit, available-case adjudication and final figures.

Excluded for size and privacy:

- 1,920 individual provider session files and raw response payloads;
- duplicated schedules, registries and package-level manifests;
- local authorization records, machine paths and credential interfaces;
- caches, bytecode and temporary scanner artifacts.

The omitted raw provider archive remains part of the separate authoritative reviewer package. Its absence from GitHub does not change the retained summaries, failure ledger or claim boundary. The GitHub projection has its own external checksum ledger in the parent staging audit and does not reuse the authoritative archive's whole-package checksum as if the trees were identical.
